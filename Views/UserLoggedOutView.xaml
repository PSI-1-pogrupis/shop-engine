<UserControl x:Class="Views.UserLoggedOutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    <Grid Background="#FF313638">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="35*"/>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="35*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="30*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>

        <Button Grid.Column="1" Grid.Row="1" Content="Login" FontSize="12" Command="{Binding ChangeViewCommand}" CommandParameter="Login"/>
        <Button Grid.Column="3" Grid.Row="1" Content="Register" FontSize="12" Command="{Binding ChangeViewCommand}" CommandParameter="Register"/>
        <TextBlock Grid.ColumnSpan="5" Text="Not logged in." FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White"/>
    </Grid>
</UserControl>
